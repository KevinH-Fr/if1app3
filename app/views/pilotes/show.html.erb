

 <% if user_signed_in? %>
    <br/>

    <%= render @pilote %>

 <%#= line_chart Resultat.where(pilote_id: @pilote).group(:pilote_id).sum(:score) %>

 <%#= continuer pour passer le graph avec en absisse les events
  puis reussir à dessiner le graph pour chaque pilote dans classement pour voir la progression
  %>


    <%#= render "chart" %>
    <%#= render "chartpie" %> <br> <hr>

     <%= render "chartpie" %> <br> <hr>

    <div>

     <% if current_user.admin? %>
       <table> 
         <tr>
            <td>  <%= link_to "", edit_pilote_path(@pilote), class:"fa-regular fa-pen-to-square btn btn-primary" %>  </td>
            <td> | </td>
            <td> <%= button_to "", @pilote, class:"fa-solid fa-trash-can btn btn-danger", method: :delete %>  </td>
         </tr>
       </table>
     <% end %>
    </div>

 <br/> 
    <%= link_to "", pilotes_path, class:"fa-solid fa-arrow-rotate-left btn btn-secondary" %> 
     
    <br/>
  
<% else %>
  <br/>
  <strong>Vous n'avez pas les droits...</strong>
<% end %>