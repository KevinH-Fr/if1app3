

 <% if user_signed_in? %>
    <br/>

    <%= render @pilote %>

    <div>

     <% if current_user.admin? %>

       <table> 
         <tr>
            <td style="padding-right: 5px;">  <%= link_to "", edit_pilote_path(@pilote), class:"fa-regular fa-pen-to-square btn btn-primary" %>  </td>
            <td style="padding: 5px;"> <%= button_to "", @pilote, class:"fa-solid fa-trash-can btn btn-danger", method: :delete %>  </td>
         
         </tr>
       </table>
     <% end %>
    </div>
 
    <%= link_to "", pilotes_path, class:"fa-solid fa-arrow-rotate-left btn btn-secondary" %> 
     
  <br/>
  
<% else %>
   <%= render "shared/demandeconnexion" %>

<% end %>