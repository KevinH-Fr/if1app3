<% if user_signed_in? %>
  <% if current_user.admin? %>
  
      <h1>Editer saison</h1>

      <%= render "form", saison: @saison %>
      <br>

      <div>
        <%= link_to "", @saison, class:"fa-solid fa-eye btn btn-primary" %> |
      </div>
      <br/>
      <%= link_to "", saisons_path, class:"fa-solid fa-arrow-rotate-left btn btn-secondary"  %>
    <% else %>
    <br/>   <strong>Vous n'avez pas les droits...</strong>
  <% end %>
<% end %>