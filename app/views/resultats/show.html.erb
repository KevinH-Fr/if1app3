 <% if user_signed_in? %>

<br>

    <%= render @resultat %>

    <div>
        <br/> <hr/>
    
        <%= link_to "", resultats_path(saisonId: @saisonId, divisionId: @divisionId, eventId: @eventId, numGp: @numGp), class:"fa-solid fa-arrow-rotate-left btn btn-secondary" %> 

        <% if current_user.admin? %>
            <%= link_to "", edit_resultat_path(@resultat), class:"fa-regular fa-pen-to-square btn btn-primary" %> |
            <%= button_to "", @resultat, class:"fa-solid fa-trash-can btn btn-danger", method: :delete, :onclick => "return confirm('Sûr ?')"  %>
        <% end %>

    </div>

<% else %>
  <br/>
   <%= render "shared/demandeconnexion" %>
<% end %>