 <% if user_signed_in? %>

<br>

    <%= render @resultat %>

    <div>
        <br/> <hr/>
    
    <% @eventId = session[:event] %>
    <% @saisonId = Event.find(session[:event]).saison_id  %>
    <% @divisionId = Event.find(session[:event]).division_id  %>
  
    <%= link_to "", resultats_path(saisonId: @saisonId, divisionId: @divisionId, eventId: @eventId), class:"fa-solid fa-arrow-rotate-left btn btn-secondary" %> <br><br>

        <% if current_user.admin? %>
         <table> 
           <tr>
             <td>
                <%= link_to "", edit_resultat_path(@resultat), class:"fa-regular fa-pen-to-square btn btn-primary" %>
             </td>   
             <td> | </td> 
            <td>  
                <%= button_to "", @resultat, class:"fa-solid fa-trash-can btn btn-danger", method: :delete, :onclick => "return confirm('Sûr ?')"  %>
             </td> 
        <% end %>

    </div>

<% else %>
  <br/>
   <%= render "shared/demandeconnexion" %>
<% end %>