<p style="color: green"><%= notice %></p>

<%= render @reglement %>

 <%= link_to "", reglements_path, class:"fa-solid fa-arrow-rotate-left btn btn-secondary" %>

  <% if current_user.admin? %>
    <table> 
      <tr>
        <td>
          <%= link_to "", edit_reglement_path(@reglement), class:"fa-regular fa-pen-to-square btn btn-primary" %>
        </td>   
        <td> | </td> 
        <td>  
          <%= button_to "", @reglement, class:"fa-solid fa-trash-can btn btn-danger", method: :delete, :onclick => "return confirm('Sûr ?')"  %>
        </td> 
      </tr>
    </table>
  <% end %>