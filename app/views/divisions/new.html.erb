 <% if user_signed_in? %>
    <% if current_user.admin? %>
      <h1>Nouvelle division</h1>
      <%= render "form", division: @division %> <br>
      <div>
        <%= link_to "Retour divisions", divisions_path, class:"btn btn-secondary" %>
      </div>
  <% else %>
    <br/> <strong>Vous n'avez pas les droits...</strong>
  <% end %>
<% end %>