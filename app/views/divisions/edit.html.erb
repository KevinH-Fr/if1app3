 <% if user_signed_in? %>
  <% if current_user.admin? %>

      <h1>Editer division</h1>

      <%= render "form", division: @division %>

      <br>

      <div>
        <%= link_to "", @division, class:"fa-solid fa-eye btn btn-primary" %> |
        <%= link_to "", divisions_path, class:"fa-solid fa-arrow-rotate-left btn btn-secondary"  %>
      </div>
    <% else %>
    <br/>   <strong>Vous n'avez pas les droits...</strong>
  <% end %>
<% end %>