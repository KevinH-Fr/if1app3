<h1>Editing pari</h1>

<%= render "form", pari: @pari %>

<br>

<div>
  <%= link_to "", pari_path(@pari), class:"fa-regular fa-eye btn btn-primary" %> |
  <%= link_to "", paris_path(saisonId: @saisonId, divisionId: @divisionId, eventId: @eventId, numGp: @numGp), class:"fa-solid fa-arrow-rotate-left btn btn-secondary" %> <br><br>
 
</div>
